#include<stdio.h>
int main()
{
    int l,b;
    float p;
    printf("enter a length and breadth of a rectangle:");
    scanf("%d%d",&l,&b);
    p =(2*(l+b));
    printf("the perimeter of rectangle is %f", p);
    
}
